# 7zplus v0.2.2

- 修复安装器在清理旧版资源管理器扩展后误报“安装中止”的问题。卸载程序和注册表步骤现在分别检查自己的执行结果。
- “关于”页现在可在应用内下载并安装更新。程序会识别安装版或绿色版，选择对应发行包，校验 SHA-256，退出后完成更新并重启；失败时尝试恢复原版本。
- 更新下载支持取消，三种语言的状态与错误提示已同步。

提供 Windows x64 安装版、绿色版和 `SHA256SUMS.txt`。**从 v0.2.1 升级需要手动安装或解压一次 v0.2.2**；应用内更新能力从 v0.2.2 开始用于后续版本。

验证范围：Windows x64 完整构建、发行包内容和校验值。尚未执行实际跨版本安装或桌面交互验证。

---

Fixes an installer false failure after retiring old Explorer extensions. About now downloads the package matching the current installed or portable copy, verifies its SHA-256, applies the update after exit, and restarts. Download cancellation and rollback are included. Upgrading from v0.2.1 requires one manual installation or extraction; in-app updates are available for later releases. The Windows build and package hashes were verified, but a live upgrade was not exercised.
